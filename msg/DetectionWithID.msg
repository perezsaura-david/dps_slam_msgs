# A single detection: an instance id, a semantic label, and a geometry.

string   id        # Instance / data-association id
string   label     # Semantic class (e.g. "aruco", "gate", "door")
Geometry geometry  # Geometric representation of the detection
